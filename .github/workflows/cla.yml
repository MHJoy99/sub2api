name: "CLA Assistant"

# on: [disabled]
on: workflow_dispatch

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  cla-check:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
